---
name: wwyw2-design-direction
description: Locked design direction for the wewatchyourwebsite.com (wwyw2) rebuild
metadata:
  type: project
---

Building **wewatchyourwebsite.com** (project dir `wwyw2`) as a clean static site (semantic HTML + shared CSS/JS, deploy target Cloudflare Pages). Goal set 2026-05-30.

**Design formula: WWYW brand palette × Cloudflare structural vibe × lime-themed LetterGlitch accents.** Brief says "similar (NOT same)" to the May 2026 Cloudflare redesign — a low-precision target, so borrow STRUCTURE, not Cloudflare's palette.

**REFERENCE RULES (user-stated):** (1) The REAL wewatchyourwebsite.com = CONTENT reference ONLY — its design is bad (red-on-navy, flat/cramped), IGNORE its visuals. Use its substance: 6 hero stats incl. killer 0.047% re-infection rate, 8M cleaned since 2007, 2.9M monitored today, 300k malware samples; real 2025 incident examples (inotify rootkit on PanelAlpha, casino SEO spam in Elementor JSON, webanalytics-cdn.sbs on GridPane, GLASSWORM Unicode steganography); second product = Code Analyzer (8-stage pipeline: secrets, static/Semgrep, dependency/OSV, LLM review, bundle, unicode-stego, adversarial verify, attack-chain); 72.8% of 2096 AI apps had a vuln; hosting partners (GridPane/PanelAlpha/RunCloud/xCloud/Hetzner). (2) DESIGN caliber reference = ChameleonLanding (`../ChameleonLanding`) + Cloudflare — esp. CUSTOM ANIMATED OBJECTS (Chameleon's hero is a dependency-free WebGL radar shader in radar.js — GLSL fragment shader, IntersectionObserver pause, reduced-motion fallback to static SVG rings). That caliber = the bar.

**User feedback trajectory (why iterations happened):** floor-raising (type→surfaces→motion) wasn't enough; root issue was COMPOSITION (every section same vertical rhythm) + BASIC content (generic invented copy vs real specifics). Fix = real substance + bold asymmetric composition + genuine custom animated centerpiece(s). User: "idea is great, needs many small improvements to be top-tier, you're the expert — you decide." NOT a teardown; an execution-quality pass.

**Brand palette (from logo SVG `assets/img/*.svg`, ground truth):**
- Lime/chartreuse `#69ff12` (primary accent — the shield scanline glyph)
- Green radial gradient `#bed143` → `#3d6f3c`
- Navy/near-black shield `#000033` / `#0c0c0c`
- Wordmark reads "WEWATCHYOURWEBSITE". Security/monitoring brand (shield + scanline aesthetic).

**Cloudflare vibe = structure not color:** generous whitespace, big confident type, clean sectioning, CSS-based minimalism (no heavy drop-shadow/noise images — crisp gradients), modular lock-together components, responsive grid, custom icon system, restrained motion. (Source: blog.cloudflare.com/redesigning-cloudflare)

**LetterGlitch** (reactbits, full source ported to VANILLA canvas — no React in a static site): re-theme default greens to lime/teal brand. Use as TASTEFUL ACCENT (hero backdrop, section dividers, footer) in low-text zones only with vignette on — NOT literally behind body text everywhere (kills readability, fights clean vibe).

**Assets:** 4 Lottie animations (~600×400 @60fps) = the 4 service pillars: file-integrity, malware-removal, root-cause-determination, total-protection. Lottie requires HTTP to load (CORS) — `npm run dev` serves on port 8098.

**Anti-drift workflow** (wwyw history: 3 prior attempts failed by drifting design): 1) synthesize design system, 2) build ONE homepage to high bar emitting shared files (tokens CSS, LetterGlitch module, header/footer), LOCK it, 3) THEN fan out remaining pages off locked CSS/components, 4) cross-page consistency review. Homepage-first is the anti-drift anchor.

**STATUS (commit f65c5e4):** Landing page BUILT — `index.html` (7 sections: hero, proof ribbon, 2x2 services grid w/ Lottie, how-it-works, stats, pricing teaser, FAQ, final CTA), `css/styles.css` (locked design tokens), `js/letter-glitch.js` (vanilla port), `js/main.js`. CTA decided: primary "Protect My Site", secondary "Run a Free Scan". 4-lens adversarial review ran; all 21 findings fixed (hero/footer/CTA scrim AA contrast, FAQ focus ring, forced-colors mode, mobile-menu inert focus containment, perf reflow, teal two-accent, etc). All assets serve HTTP 200, JS syntax-checks pass.

**VISUAL PASS DONE (via Chrome MCP, this session):** Verified live at http://localhost:8098 at 1440px + 390px. Hero/final-CTA/footer = glitch reads as signature texture AND text is crisp (scrim balance correct, NOT buried). Services Lottie animations render. Mobile responsive (hamburger, stacked CTAs, reflow) is clean. ZERO console errors.

**Logo FIXED:** the original metal-gradient/multiply shield read as a muddy grey blob on dark. Replaced the `#wwyw-wordmark` <symbol> in index.html with a purpose-built REVERSED dark-surface logo: `assets/img/wordmark-reversed.svg` — simplified dark shield + lime hairline + lime radial core + lime scanline glyph + light wordmark text. Reads crisply at 32px header + footer. Also added `assets/img/favicon.svg` (lime shield) — killed the favicon 404.

**CONTENT-FIDELITY PASS DONE (2026-05-31):** User flagged the build had drifted to invented copy instead of copying real wewatchyourwebsite.com content (source: `C:\Users\brayd\Downloads\wwyw-content.txt`). Corrected: (a) DROPPED all fabricated "money-back guarantee" claims (was ~6×, nowhere in source) — kept truthful "no per-clean fees"; (b) FIXED pricing to source ($59.95/yr WordPress Protection for site owners + enterprise/hosting custom; Code Analyzer "Free first scan" + API/white-label) — was invented $0/$39.95/$199.95 tiers; (c) hero reworked to DUAL-PRODUCT framing (protect WordPress + analyze AI code) w/ 6 real stats (18, 8M+, 2.9M, 300k+, 0.047%, 2096). ADDED the missing SECOND PRODUCT: Code Analyzer section w/ custom 8-stage pipeline centerpiece (`js/pipeline.js` — IntersectionObserver lights stages in sequence, accumulates, reduced-motion lights all). ADDED: "Why This Is Different" + 4 real 2025 incidents (inotify rootkit/PanelAlpha, casino SEO/Elementor JSON, webanalytics-cdn.sbs/GridPane, GLASSWORM), Hosting Partners (GridPane/PanelAlpha/RunCloud Active, xCloud Available, Hetzner Active, API/Custom), Blog teaser (3 real posts). FAQ kept but money-back answer scrubbed. New CSS appended to styles.css (hero-stats, analyzer/pipeline, incidents, partners, pricing-grid--duo, blog/post). Nav + footer relinked to product anchors (#wordpress/#analyzer/#why/#partners/#pricing). Verified live 1440+390px, all 8 pipeline stages light, ZERO console errors. Committed (index.html, css/styles.css, js/pipeline.js staged by name).

**2026 POLISH PASS DONE (2026-05-31, via hallmark audit):** User asked to "refine/upgrade to 2026 standards" — ran `hallmark audit` (read-only) + Lighthouse baseline first, confirmed it's POLISH not redesign (user previously called the design "great"; 3 prior attempts failed by drifting). Audit found 1 critical + 2 major + 3 minor. Fixed: (1) CRITICAL contrast — footer `.btn--primary` inherited muted-grey footer-link color over lime bg (2.31:1 fail); scoped `.footer-col__list a:not(.btn)` so CTAs keep `--text-on-accent`. (2) MAJOR "eyebrow on every section" tell — cut 8 eyebrows → 1 (kept only hero lead line "18 Years of Real-World Threat Intelligence"); section h2s now carry identity (more editorial, less templated); also killed the duplicated "Stop reacting. Start preventing." eyebrow. (3) deleted dead `--ease-spring` overshoot token + aligned `--ease-out` to Hallmark spec `(0.16,1,0.3,1)`. (4) gave the hero's killer 0.047% stat a full-height glowing lime cap (others keep 20px stub caps) — quiet emphasis on the brand's best number, no card treatment. Hover language already a deliberate 2-tier system (buttons/small-cards -2px, large cards -4px) — left as-is. Infinite "live/scanning" loops are theme-justified status signals — left. RESULT: **Lighthouse 100/100/100/100** (A11y 96→100), zero console errors, verified desktop+mobile. Scope chosen by user: "audit fixes + tasteful polish" (declined bigger "visible refresh"). Committed (index.html, css/styles.css by name).

**ANALYZER PIPELINE → "LIVE SCAN ENGINE" PASS DONE (2026-05-31):** User goal: upgrade the "Your AI wrote the code. Did it write in the backdoor?" (#analyzer) section to best-possible-unique 2026. Advisor steered scope correctly: the pipeline FIGURE is the section-local centerpiece (= the stated Chameleon-radar caliber bar) — elevate THAT, do NOT touch the 4 stat cards (site-wide `.analyzer-stat`/`.stat`/`.partner`/`.post` shared recipe — restyling here only = cross-section drift, the exact failure that killed 3 prior attempts) and do NOT "fill" the right-of-headline space (it's the live LetterGlitch canvas = signature texture, confirmed via getComputedStyle, NOT a void). User picked "you decide — push furthest" → built the **live scan-engine** direction: kept the `<ol>` + all copy (no-JS readable), added per-stage finding readouts (`3 found`/`clean`/`1 high`/`2 flagged`/`1 hidden`/`7 confirmed`/`2 chains`) as mono chips that reveal as scanned; lime chip=`hit`, muted=`clean`, teal=`verify` (stays on the LOCKED two-accent palette — NO new red/amber severity color introduced, severity expressed via the existing lime=caught-it semantics); a glowing scan **beam** (`.pipeline__beam`, positioned by JS via `--beam-top`) physically descends the rail igniting nodes/rails/findings in turn; output node ignites `is-complete` once all scanned; number findings count up. `js/pipeline.js` rewritten: one rAF pass, single up-front getBoundingClientRect measure (no per-frame reflow), reduced-motion → settleAll (lights all + findings shown + no beam). VERIFIED via Chrome MCP: desktop 1440 (mid-scan beam visible between stages, end-state all-lit+complete), mobile 390 (chips wrap via flex-wrap on `__head`, no overflow), reduced-motion (8 lit immediately, never scanning, count set direct), ZERO console errors, **CLS 0.00 / LCP 290ms**, **Lighthouse A11y/BP/SEO/Agentic all 100**. Committed (index.html, css/styles.css, js/pipeline.js by name).

**ANALYZER FIGURE → FULL RECONCEPTION ("code reveals its threats", 2026-05-31):** User rejected the scan-engine pass: "I barely notice a change... basic idea a junior designer would make." Correct — it was a styled vertical timeline (a list with dots), not in the same league as the project's real bar (`defense-field.js` = a custom 2D-canvas SCENE: perspective slab, attack vectors bursting on a shield line). Advisor steered the fix sharply: the problem is CONCEPT not rendering-tech — do NOT canvas-ify (the 8 stages are load-bearing TEXT for a11y/SEO/no-JS; defense-field is pure aria-hidden illustration with zero text, so the analogy is false). Keep real stage content in DOM, wrap a genuinely different SPATIAL idea around it. Confirmed concept WITH user (lesson from the rejected blind "you decide" round): picked **"Code that reveals its threats"** — the figure IS the artifact under analysis. LEFT = a code-editor panel (`auth.middleware.js`, "vibe-coded · unreviewed") holding a realistic snippet that looks clean; a scan sweep descends and HIDDEN THREATS SURFACE IN PLACE (hardcoded API_KEY L4, vulnerable `analytics.min.js` import L2, `eval()` L9, SQLi `runQuery` L12, and the **zero-width GLASSWORM glyph L7 — invisible at rest, expands to a visible lime ⌫ marker when surfaced**, the headline's "hidden character" made literal). RIGHT = the findings ledger (the 8 real stages, DOM text) filling 01→08 with line-referenced findings (`L4 · 1 key`, `L9 · eval()`, `L2 · 1 high`, `L12 · SQLi`, `L7 · 1 hidden`), ending in an assembled "verified vulnerability report" verdict w/ check badge. New classes `.inspect*` replaced all `.pipeline*`; `js/pipeline.js` rewritten (selector now `[data-inspect]`). KEY IMPL LESSON (advisor catch, pre-commit): ledger lighting MUST be DECOUPLED from code-line flagging — first attempt ordered ledger stages by their code-line Y, which scrambled the reveal (04 lit before 01); fixed by lighting the ledger strictly in listed order on the eased progress timeline while flagging code lines independently by sweep-Y (no connector drawn between them, so no need to sync). Also made the stage→line mapping SECURITY-ACCURATE (eval→Static Analysis not "clean"; vulnerable import→Dependency Audit not the SQLi line) — security-literate audience + user already rejected inaccurate content once; numbers reconcile (5 line findings → "5 confirmed"). Dropped the literal U+200B from HTML source (fragile; the visible glyph comes from CSS `::after{content:"⌫"}`). Token-only (syntax colors map to lime/teal/white; no new hex). VERIFIED via Chrome MCP: desktop 1440 (sweep + progressive surfacing on ORGANIC load, lighting order strict 0→7, lines 2/4/7/9/12 flag), mobile 390 (panels stack, code scrolls internally, figure contained — page has NO horizontal scroll; the scrollWidth=780 reading is the off-canvas fixed `.mobile-menu`, body has overflow-x:hidden), reduced-motion (all resolved immediately, no sweep), ZERO console errors, **CLS 0.00 / LCP 222ms**, **Lighthouse 100 ×4**. Committed (index.html, css/styles.css, js/pipeline.js by name). [Supersedes the scan-engine pipeline pass above.]

**ANALYZER COMPOSITION FIX (2026-05-31):** User on the inspection figure: "great direction but 2 containers next to each other looks weird esp. because the right one is taller... and 4 containers above so it's a lot of containers making the layout basic/outdated." Two fixes, both compositional (NOT a figure redo): (1) **De-boxed the stat row** — `.analyzer-stats` dropped borders/backgrounds → inline figures with hairline `border-left` separators (suppressed at row starts; 2×2 on mobile, 4-up desktop). Confirmed `.analyzer-stat` is LOCAL to this section (13 uses, index.html only — NOT the shared `.stat`/`.partner`/`.post` recipe) so no cross-section drift. Bumped label color `--text-muted`→`--text-secondary` because de-boxed labels now sit directly over the live LetterGlitch (no card scrim); verified AA: worst-case contrast over the brightest possible lime glitch pixel @0.3 opacity = **4.87:1** (passes 4.5 normal-text). (2) **Merged the two figure cards into ONE window** — advisor's key point: the win is SHARED CHROME not a closed gap. New structure: `.inspect` = single bordered frame; `.inspect__chrome` title bar spans full width; `.inspect__body` grid (code | seam | panel); `.inspect__code` lost its own border/radius (now a region); `.inspect__panel` lost its card → seam is its leading edge (`border-left` desktop / `border-top` mobile). Height mismatch resolves itself: one frame stretches both cells to the taller (ledger) height, and the code pane fills trailing space with editor background below line 12 (like a real IDE — no stretch/scroll hack). Reads as one tool, not two cards. HTML: added `.inspect__chrome` + `.inspect__body` wrapper (closing `</div>` before `.inspect` close). Animation selectors (`[data-scan]`, `.inspect__pre`, `.inspect-stage`, `.cl[data-line]`) survived untouched. VERIFIED via Chrome MCP: desktop 1440 (one window, seam clean, code fills trailing height; progressive scan still lights 0→7 in order, lines 2/4/7/9/12 flag, on ORGANIC below-fold load), mobile 390 (stats 2×2, window stacks w/ top-border seam, contained, body overflow-x:hidden), zero console errors, **CLS 0.00 / LCP 196ms**, **Lighthouse 100 ×4**. NOTE: browser aggressively caches styles.css — use ignoreCache reloads when verifying CSS changes. Committed (index.html, css/styles.css by name; pipeline.js untouched).

**ANALYZER LEDE REWRITE (2026-05-31):** User: "heading and description need optimizing for a better section transition." Real issue (advisor): the old lede recited "72.8% / 1 in 4" which now DUPLICATE the de-boxed stat row 3 lines below it. Fix = lede frames the problem, stat row carries the numbers. KEPT the h2 untouched ("Your AI wrote the code. Did it write in the backdoor?" — strongest hook in the section; user said "looks good but"; keeps its id so aria-labelledby intact). Rewrote ONLY the lede → "Vibe-coded apps ship fast — and ship with exploitable flaws that look like ordinary code. A hardcoded key, an injection sink, a character you can't even see. We read the source the way an attacker would, and surface what the model left behind." This does the transition work in prose (light thematic pivot to AI/vibe-coded code — NOT a heavy "product two" bridge; hero already established dual-product framing) and SETS UP THE ARTIFACT below — the three threats it names are exactly what the code window then demonstrates (hardcoded key L4, injection L9/L12, invisible char L7). Held constraints: no eyebrow reintroduced (2026 pass cut those), no stats in lede (row owns them), stayed concrete (avoided generic "is your AI code safe?" drift). Copy-only — verified desktop+mobile balance (head not thin, flow heading→lede→stats→window reads coherently); skipped Lighthouse/CLS battery (nothing structural moved). Committed (index.html by name).

**ANALYZER SECTION-TRANSITION FIX (2026-05-31):** User wanted a better *design* (visual) transition, not copy — the analyzer met its neighbours at HARD horizontal seams. Root cause (advisor): a flat-colour STEP, not the texture. The analyzer is transparent over body `--bg-base` (#05060a) and its `__bg-fade` faded edges to `--bg-base`, but BOTH neighbours (`.how` before, `.why` after) are `.section--alt` = `--bg-base-2` (#080a11) — so the section resolved to the wrong tone exactly where it butted the next section = the visible line. Fix lives ENTIRELY inside `.analyzer` (it already has overflow:hidden + own bg layers — NO touching neighbours/tokens/`.section--alt`, the documented drift trap): (1) `.analyzer{ background:var(--bg-base-2) }` to match neighbours (zero flat-colour step at either seam), (2) `__bg-fade` top/bottom stops `--bg-base`→`--bg-base-2`, and pushed the ramp stops outward (30/70 → 22/78) so the glitch EMERGES/DISSOLVES over more distance instead of snapping on. Edge-only feather — mid-section glitch opacity UNCHANGED so the verified stat-label AA contrast (4.87:1) still holds. VERIFIED both seams (into analyzer from .how, out to .why) at desktop+390: no horizontal line, texture feathers smoothly, mid-section figure unchanged. Paint-only (no layout/JS) → skipped Lighthouse/CLS. Committed (css/styles.css by name).

**ANALYZER HEADER CENTERED (2026-05-31):** User's actual "weird transition flow" complaint = the LEFT-ALIGNED heading+lede (`.section-head` max-width:640px) left a big void on the right at desktop, then the stat row + full-width window below span the whole 1240px container → cramped-left-header → abrupt-full-width-content. Fix (advisor, Option A over a 2-col header): center the head — ONE class change `section-head reveal` → `section-head section-head--center reveal`, reusing EXISTING CSS (`.section-head--center` already centers title+lede via margin-inline:auto+text-align:center). Bonus: the section directly above (`.how`) is ALSO `section-head--center`, so this gives consistent centered→centered section rhythm (not centered-then-lopsided) — anti-drift (reuse, no new pattern). Rejected 2-col heading-left/lede-right: novel pattern (drift) + new grid/mobile CSS + risks reading as two disconnected blocks across 1240px. Centered head sits above full-width blocks whose visual center = container center, so it aligns. VERIFIED desktop 1440 (void gone, header balanced above stat row+window) + mobile 390 (centering collapses harmlessly on full-bleed column, unchanged). One-class align-only → skipped Lighthouse/CLS. Committed (index.html by name).

**WHY SECTION → "INCIDENT LEDGER" PASS DONE (2026-05-31):** User: "in a similar way that we improved the analyzer section, do the same for the 'We didn't start with a product. We started with the attacks.' (`#why`) section." Old state = the SAME junior pattern we'd just fixed: left-aligned header (right-side void) + a 2×2 grid of FOUR identical bordered `.incident` cards (each just year "2025" + a paragraph + left accent bar — no hierarchy, no narrative). Advisor's load-bearing steer: `#why` is SANDWICHED between two animated accent-heavy signature visuals (analyzer scan window above, `#stats` scan-radar canvas + count-up numbers below) — so the distinguishing move is **RESTRAINT**, NOT a third light show. A quiet typographic provenance artifact CONTRASTS and lands; a third animation blurs all three together. Resolved the data fork WITH the user (the all-`2025` data kills a literal "since 2007" chronology — won't fabricate years for real IR cases): user picked **evidence-log** over real-chronology / span-framing. Built **one incident-ledger artifact** (de-box move, mirrors the analyzer merging two boxes into one window): `.ledger` = single bordered frame; `.ledger__chrome` titled strip ("INCIDENT LEDGER" + teal mark + "Active IR · field notes" meta) reusing the analyzer's `.inspect__chrome` vocabulary so they read as siblings; `.ledger__rows` is the SAME `<ol>`/`<li>` (no-JS readable, kept list semantics) with each row COLUMNAR — `[case-ID + threat-class] | description | status-pill` — which is what EARNS the 1080px width (advisor's real-risk catch: a single column of paragraphs stretched to 1080px reads badly >75ch; columnar rows keep prose to a readable measure). Synthetic case IDs `IR-2025-041/039/044/047` (user pre-approved a preview containing them; did NOT invent additional checkable facts — no real dates/CVEs/victims). GLASSWORM row = `.ledger-row--key` w/ teal leading edge + teal status pill, tying back to the analyzer's "hidden character" through-line. Status pills (`Dismantled`/`Remediated`/`Reported`/`Documented`) are RESOLUTION not severity — neutral mono by default, teal on the key row only, NO success-green/danger-red (the easiest place to leak a third color — held the two-accent palette). Header centered (`section-head--center`, same one-class reuse as the analyzer) — justified here because the full-width ledger below is symmetric (advisor: center only when content below is symmetric; it is). **STATIC by design — NO new JS, no scan, no counters, no rAF** (restraint IS the deliverable). Mobile: rows stack (id→class→desc→pill), chrome meta hidden <520px so "INCIDENT LEDGER" keeps one line. Anti-drift verified: `.incident`/`.incidents` were LOCAL to index.html (grep: 1 file, no JS) before restyle; `.link-arrow` left untouched (shared). VERIFIED via Chrome MCP: desktop 1440 (one cohesive artifact, columnar rows aligned, void gone, GLASSWORM teal edge reads), mobile 390 (clean stack, chrome one line), ZERO console errors, **CLS 0.00 / LCP 278ms**, **Lighthouse 100 ×4**. Committed (index.html, css/styles.css by name; no JS touched).

**Known follow-ups (deferred, non-blocking):** export a 1200x630 raster OG/social share image (SVG OG won't render on social); wire Privacy/Terms placeholder links + real signup/scan/blog CTA URLs when backend ready (blog links point to blog.wewatchyourwebsite.com placeholder); consider self-hosting lottie.min.js + fonts for Cloudflare Pages. Then: build remaining multipage site pages off this LOCKED anchor.
